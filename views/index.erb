<!DOCTYPE html>
<html>
<head><title>Hoster</title></head>
<link href="css/style.css" rel="stylesheet" type="text/css">
<link rel="stylesheet" href="http://yui.yahooapis.com/pure/0.4.2/pure-min.css">
<body>
<h1 class="Three-Dee">ⓗoster</h1>

<form class="pure-form" style="text-align: center" action="/new_party" method="post">
  Party Name: <input style="width: 9em" type="text" name="party_name">
  Party Size: <input style="width: 4em" type="number" name="party_size">
  Phone Number: <input style="width: 9em" type="number" name="party_phone">
  <button type="submit" class="pure-button pure-button-primary">Submit</button>
</form>
<table class="pure-table pure-table-bordered" align="center" style="margin-top:5%; background-color:whitesmoke">
  <thead>
  <tr style="text-align: center">
    <th>Party Name</th>
    <th>Size</th>
    <th>Phone</th>
    <th>Wait Time</th>
    <th>Seat / Text Party</th>
  </tr>
  </thead>
  <tbody>
  <% parties.each do |party| %>
      <tr>
        <td><%= party.name %></td>
        <td><%= party.size %></td>
        <td><%= party.phone %></td>
        <td>
          <% if party.has_wait_time? %>
              <%= party.wait_time %> minutes
          <% end %>
        </td>
        <td>
          <form style="display: inline" action="/delete/<%= party.name %>" method="post">
            <button class="remove-button"
                    type="submit">Remove
            </button>
          </form>
          <form style="display: inline" action="/text/<%= party.phone %>" method="post">
            <button class="text-button"
                    type="submit">Text Party
            </button>
          </form>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>
</body>
</html>